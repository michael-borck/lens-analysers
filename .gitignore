# lens-analysers is a DOCS-ONLY umbrella. It now lives as its own folder inside a
# `lens/` workspace, alongside each analyser/app repo (they are siblings, not nested
# here), so there are no nested projects to ignore — just track the docs and skip junk.

# Junk
__pycache__/
*.py[cod]
.venv/
.DS_Store
docs/rendered/
